# Fault Injection for TFLite CNNs (TFM)

Biblioteca **modular** para inyección de fallos en **TFLite** y protocolo reproducible para evaluar **robustez** en modelos CNN (experimentos **SBF**, **Bit-Sweep**, **MBF**) sobre representaciones **FP32**, **CH** e **INT8**.

Modelos: **LeNet-5** (entrenado desde cero en CIFAR-10), **MobileNetV2**, **EfficientNet-B0**, **GoogLeNet/Inception** (subset ImageNet-V2).

## Estructura
